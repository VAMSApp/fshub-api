import { FSHubConfig } from './types';
export default class FSHubApi {
    private axios;
    private config;
    constructor(config: FSHubConfig);
    getCurrentPilot(): Promise<any>;
}
//# sourceMappingURL=index.d.ts.map